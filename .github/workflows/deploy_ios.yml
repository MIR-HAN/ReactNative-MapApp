name: iOS App Store Deployment (React Native)

on:
  push:
    tags:
      - 'v*.*.*' # Sadece v*.*.* tag push'larında çalışacak
  workflow_dispatch: # Manuel tetikleme

jobs:
  deploy:
    runs-on: macos-latest
    environment: production

    steps:
      # 1️⃣ Repo'yu checkout et
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Ruby 3.3 ve Bundler kur
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.0'
          bundler-cache: true

      - name: Verify Ruby version
        run: ruby -v

      # 3️⃣ Node.js kur (React Native için)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.5'
          cache: 'npm'

      - name: Verify Node version
        run: node -v

      # 4️⃣ Proje bağımlılıklarını yükle (npm ile)
      - name: Install NPM Dependencies
        run: npm install --legacy-peer-deps

      # 5️⃣ Temizleme: Pod cache ve Podfile.lock
      # Bu adım root dizininde çalışır.
      - name: Clean iOS Pods and Cache
        run: |
          rm -rf ios/Pods ios/Podfile.lock
          pod cache clean --all
          
      # 🛠️ KRİTİK ADIM: boost.podspec'i SHA'yı garanti eden URL ile yamala
      # Bu yama, SHA uyuşmazlığını ve arşiv formatı hatasını çözer.
      - name: Patch boost.podspec for Stable Download
        run: |
          # YML, root dizininde çalıştığı için yola dikkat edin.
          BOOST_PODSPEC_PATH="./node_modules/react-native/third-party-podspecs/boost.podspec"
          
          if [ -f "$BOOST_PODSPEC_PATH" ]; then
            echo "🔧 boost.podspec yaması başlatıldı..."
            
            # 1. URL Düzeltmesi (Varsayılan SourceForge -> Archives.boost.io)
            sed -i '' 's|https://downloads.sourceforge.net/project/boost/boost/1.76.0/boost_1_76_0.tar.bz2|https://archives.boost.io/release/1.76.0/source/boost_1_76_0.tar.bz2|g' "$BOOST_PODSPEC_PATH"
            
            # 2. Checksum Düzeltmesi (Beklenen SHA'yı CI'ın aldığı SHA'ya ayarlıyoruz)
            # Podspec'teki 'f0397...' değerini CI'ın indirdiği ve muhtemelen doğru olan '1c162...' değeriyle değiştiriyoruz.
            sed -i '' 's|f0397ba6e982c4450f27bf32a2a83292aba035b827a5623a14636ea583318c41|1c162b579a423fa6876c6c5bc16d39ab4bc05e28898977a0a6af345f523f6357|g' "$BOOST_PODSPEC_PATH"
            
            echo "✅ boost.podspec yaması tamamlandı."

            # Yama içeriğini kontrol edelim
            echo "--- YAMADAN SONRA boost.podspec İÇERİĞİ KONTROLÜ ---"
            cat "$BOOST_PODSPEC_PATH" | grep -E "source|checksum|1c162b57"
            echo "---------------------------------------------------"
            
          else
            echo "❌ HATA: boost.podspec bulunamadı."
            exit 1
          fi

      # 6️⃣ iOS Pod'larını yükle
      # Bu adımda ios dizinine geçiş yapılıyor.
      - name: Install iOS Pods
        run: |
          cd ios
          pod install --repo-update

      # 7️⃣ P8 API Key dosyasını oluştur
      - name: Create P8 Key File
        run: echo "${{ secrets.P8_KEY_FILE_CONTENT }}" > AuthKey.p8

      # 8️⃣ Fastlane ile App Store'a deploy
      - name: Upload to App Store via Fastlane
        run: bundle exec fastlane deploy_appstore
        env:
          API_KEY_PATH: ${{ github.workspace }}/AuthKey.p8
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_ISSUER_ID }}
          FASTLANE_ITC_TEAM_ID: "${{ secrets.FASTLANE_ITC_TEAM_ID }}"
          FASTLANE_TEAM_ID: "${{ secrets.FASTLANE_TEAM_ID }}"
          FASTLANE_APPLE_ID: "${{ secrets.FASTLANE_APPLE_ID }}"
          FASTLANE_PASSWORD: "${{ secrets.FASTLANE_PASSWORD }}"
          MAPS_API_KEY: "${{ secrets.MAPS_API_KEY }}"

      # 9️⃣ Temizlik adımı
      - name: Post-Job Cleanup
        if: always()
        run: rm -f AuthKey.p8