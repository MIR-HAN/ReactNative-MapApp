name: iOS App Store Deployment (React Native)

on:
  push:
    tags:
      - 'v*.*.*' # Sadece v*.*.* tag push'larında çalışacak
  workflow_dispatch: # Manuel tetikleme

jobs:
  deploy:
    runs-on: macos-latest
    environment: production

    steps:
      # 1️⃣ Repo'yu checkout et
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Ruby 3.3 ve Bundler kur
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.0'
          bundler-cache: true

      - name: Verify Ruby version
        run: ruby -v

      # 3️⃣ Node.js kur (React Native için)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.5'
          cache: 'npm'

      - name: Verify Node version
        run: node -v

      # 4️⃣ Proje bağımlılıklarını yükle (npm ile)
      - name: Install NPM Dependencies
        run: npm install --legacy-peer-deps

      # 5️⃣ Temizleme: Pod cache ve Podfile.lock
      - name: Clean iOS Pods and Cache
        run: |
          rm -rf ios/Pods ios/Podfile.lock
          pod cache clean --all
          
      # 🛠️ NİHAİ YAMA: Checksum'u DEVRE DIŞI BIRAK ve GitHub Mirror'a GEÇ (Sözdizimi Düzeltildi)
      - name: Patch boost.podspec to GitHub Mirror and Disable Checksum (FIXED)
        run: |
          BOOST_PODSPEC_PATH="./node_modules/react-native/third-party-podspecs/boost.podspec"
          
          if [ -f "$BOOST_PODSPEC_PATH" ]; then
            echo "🔧 boost.podspec yaması başlatıldı (Sözdizimi Düzeltildi)..."
            
            # 1. Checksum satırını Podspec'ten SİL (Bu, validasyonu atlatır!)
            # 'd' komutu ile silme yapılıyor.
            sed -i '' '/:sha256/d' "$BOOST_PODSPEC_PATH"
            sed -i '' '/:checksum/d' "$BOOST_PODSPEC_PATH"
            
            # 2. URL Düzeltmesi (Sorunlu Kaynaklar -> GitHub Mirror)
            GITHUB_MIRROR_URL="https://github.com/boostorg/boost/releases/download/boost-1.76.0/boost_1_76_0.tar.bz2"
            
            # SourceForge'dan gelen linki değiştir
            sed -i '' "s|https://downloads.sourceforge.net/project/boost/boost/1.76.0/boost_1_76_0.tar.bz2|$GITHUB_MIRROR_URL|g" "$BOOST_PODSPEC_PATH"
            
            # Önceki denemelerdeki archives.boost.io linkini değiştir
            sed -i '' "s|https://archives.boost.io/release/1.76.0/source/boost_1_76_0.tar.bz2|$GITHUB_MIRROR_URL|g" "$BOOST_PODSPEC_PATH"
            
            echo "✅ boost.podspec yaması tamamlandı. Sözdizimi hatası düzeltildi."

            # Yama içeriğini kontrol edelim
            echo "--- YAMADAN SONRA boost.podspec İÇERİĞİ KONTROLÜ (SHA SATIRI YOK) ---"
            cat "$BOOST_PODSPEC_PATH" | grep -E "source|github.com"
            echo "---------------------------------------------------------------------"
            
          else
            echo "❌ HATA: boost.podspec bulunamadı."
            exit 1
          fi

      # 🛑 KRİTİK TEMİZLİK ADIMI: CI Cache'ini zorla temizleme
      - name: Force Clean Cocoapods and Repos
        run: |
          echo "Force cleaning CocoaPods system cache and updating repos..."
          rm -rf ~/Library/Caches/CocoaPods 
          pod repo update 
          
      # 6️⃣ iOS Pod'larını yükle
      - name: Install iOS Pods
        run: |
          cd ios
          pod install --repo-update

      # 7️⃣ P8 API Key dosyasını oluştur
      - name: Create P8 Key File
        run: echo "${{ secrets.P8_KEY_FILE_CONTENT }}" > AuthKey.p8

      # 8️⃣ Fastlane ile App Store'a deploy
      - name: Upload to App Store via Fastlane
        run: bundle exec fastlane deploy_appstore
        env:
          API_KEY_PATH: ${{ github.workspace }}/AuthKey.p8
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APP_STORE_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_ISSUER_ID }}
          FASTLANE_ITC_TEAM_ID: "${{ secrets.FASTLANE_ITC_TEAM_ID }}"
          FASTLANE_TEAM_ID: "${{ secrets.FASTLANE_TEAM_ID }}"
          FASTLANE_APPLE_ID: "${{ secrets.FASTLANE_APPLE_ID }}"
          FASTLANE_PASSWORD: "${{ secrets.FASTLANE_PASSWORD }}"
          MAPS_API_KEY: "${{ secrets.MAPS_API_KEY }}"

      # 9️⃣ Temizlik adımı
      - name: Post-Job Cleanup
        if: always()
        run: rm -f AuthKey.p8